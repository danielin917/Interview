#pragma once
#include<iostream>

using namespace std;

void splitter(int index, vector<int> arr);
void  swap(int& a, int& b);
void splitter(int index, vector<int> arr){
	int s_num = arr[index];
	
}
void swap(int& a, int& b){
	int z = a;
	a = b;
	b = z;
}

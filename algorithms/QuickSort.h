#pragma once


template<typenameT>
int partition(T* array, int length)
{

	pivot_val = array[length -1];
	int swap_index, check_index;
}

template<typename T>
quickSort(T* array, int length)
{
	int pivot_index = partition(

}
